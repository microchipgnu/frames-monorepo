node_modules/
dist/
.DS_Store
.env
.env.local

# Bun
.bun/

# Editor
.vscode/
.idea/

# OS
*.log

# Frames-specific regenerable artifacts (per-dataset)
**/.frame/dataset.db
**/.frame/dataset.db-*
**/.frame/rows.ndjson

# wrangler local state (miniflare KV/cache)
.wrangler/

# OpenProse run state — run traces, per-service workspaces, source caches.
# events.ndjson is the canonical state; everything under .prose/runs/ and
# top-level workspace/ is regenerable scratch.
**/.prose/runs/
**/datasets/*/workspace/
