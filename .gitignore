node_modules/
dist/
.DS_Store
.env
.env.local

# Bun
.bun/

# Editor
.vscode/
.idea/

# OS
*.log

# Frames-specific regenerable artifacts (per-dataset)
**/.frame/dataset.db
**/.frame/dataset.db-*
**/.frame/rows.ndjson

# wrangler local state (miniflare KV/cache)
.wrangler/

# OpenProse: gitignore the heavy active scratch (top-level workspace/ holds
# HTML body caches and per-service intermediates). KEEP .prose/runs/ tracked
# — the per-run trace is ~24-72KB each, makes git history the audit ledger.
**/datasets/*/workspace/
